# Security Policy

Do not report secrets by committing them or opening a public issue containing them. This repository must never contain API keys, tokens, credentials, player data, private project assets, or proprietary SDK files.

For a suspected instruction-injection, unsafe automation, credential exposure, or destructive workflow issue, provide a minimal reproduction without real secrets. Until a private reporting channel is configured, open a public issue only when the report itself contains no sensitive information.
